buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.0.0'
    }
}
apply plugin: 'com.android.application'
apply plugin: 'idea'

android {
    compileSdkVersion 'android-22'
    buildToolsVersion '23.0.2'

    defaultConfig {
        applicationId "be.fiiw.appi.oefening1"
        minSdkVersion 19
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"
    }
 
    buildTypes {
        release {
        }
    }
}

repositories {
    jcenter()
}

dependencies {
    compile 'joda-time:joda-time:2.9.2'
    compile 'com.google.android.gms:play-services-appindexing:8.1.0'
}
